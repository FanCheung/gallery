# Port for the HTTP server (default 4000)
PORT=4000

# Bind address (default 0.0.0.0)
HOST=0.0.0.0

# CORS: allowed Origin header for browser clients (default *)
# CORS_ORIGIN="http://localhost:3000"
